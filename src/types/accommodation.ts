export interface AccommodationInfo {
  id: string;
  name: string;
  address: string;
  checkIn: string;
  checkOut: string;
  bookingReference?: string;
  contactNumber?: string;
  notes?: string;
}
